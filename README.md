# Портфолио - Автосервис Mazda

Веб-приложение портфолио для автосервиса Mazda, разработанное на React с TypeScript.

## 📋 Описание проекта

Это полнофункциональное веб-приложение для демонстрации услуг автосервиса Mazda. Проект включает:
- Главную страницу с категориями услуг и моделями автомобилей
- Страницы с детальной информацией о каждой услуге
- Форму для записи на обслуживание
- Адаптивный дизайн

## 🛠 Технологии

### Frontend
- **React 18** с TypeScript
- **Redux Toolkit** для управления состоянием
- **React Router** для навигации
- **Axios** для HTTP-запросов
- **SCSS** для стилизации
- **React Hook Form** для работы с формами

### Backend
- **Express.js** - веб-сервер
- **JSON файлы** - база данных (альтернатива MongoDB для портфолио)

## 📦 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** (версия 14 или выше) - [Скачать Node.js](https://nodejs.org/)
- **npm** (обычно устанавливается вместе с Node.js) или **yarn**
- **Git** (опционально, для клонирования репозитория)

### Проверка установки

Проверьте, что все установлено корректно:

```bash
node --version
npm --version
```

Если команды возвращают версии, значит всё установлено правильно.

## 📁 Структура проекта

```
проект 2/
├── mazda-react/          # React приложение
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── Redux/       # Redux store и slices
│   │   └── assets/      # Изображения и ресурсы
│   └── package.json
├── server/               # Оригинальный сервер (MongoDB)
│   └── index.js
├── server-json/          # JSON-based сервер (для портфолио)
│   ├── index.js
│   └── package.json
└── json/                 # JSON база данных
    └── db.json
```

## 🚀 Быстрый старт

### Вариант 1: Использование JSON-сервера (рекомендуется для портфолио)

Этот вариант не требует установки MongoDB и идеально подходит для демонстрации проекта.

#### 1. Клонирование репозитория (если нужно)

```bash
git clone <ваш-репозиторий>
cd "проект 2"
```

#### 2. Установка зависимостей

```bash
# Установка зависимостей для React приложения
cd mazda-react
npm install

# Установка зависимостей для JSON-сервера
cd ../server-json
npm install
```

#### 3. Запуск JSON-сервера

```bash
# Из папки server-json
npm start
# или для разработки с автоперезагрузкой
npm run dev
```

Сервер запустится на `http://localhost:3004`

#### 4. Запуск React приложения

Откройте новый терминал:

```bash
# Из папки mazda-react
npm start
```

Приложение откроется на `http://localhost:3000`

### Вариант 2: Использование MongoDB сервера

Если у вас установлен MongoDB и вы хотите использовать оригинальный сервер:

#### 1. Установка MongoDB

Убедитесь, что MongoDB установлен и запущен на `mongodb://127.0.0.1:27017`

- [Скачать MongoDB](https://www.mongodb.com/try/download/community)
- Или установить через пакетный менеджер вашей ОС

#### 2. Установка зависимостей

```bash
# Установка зависимостей для React приложения
cd mazda-react
npm install

# Установка зависимостей для сервера
cd ../server
npm install
```

#### 3. Запуск сервера

```bash
# Из папки server
npm start
```

#### 4. Запуск React приложения

```bash
# Из папки mazda-react
npm start
```

## 📡 API Endpoints

### JSON-сервер (server-json)

- `GET /categories` - получить все категории услуг
- `GET /mazda` - получить все модели Mazda
- `POST /service` - получить детальную информацию об услуге
  ```json
  {
    "id": "64b54718663ae83b0900aad8"
  }
  ```
- `POST /entry` - создать новую заявку
  ```json
  {
    "name": "Иван Иванов",
    "phone": "+79991234567"
  }
  ```

### MongoDB сервер (server)

Те же endpoints, но данные хранятся в MongoDB.

## 📊 Структура данных

### Categories (Категории услуг)
```json
{
  "_id": "string",
  "image": "string",
  "title": "string (путь к изображению)"
}
```

### Mazda (Модели автомобилей)
```json
{
  "_id": "string",
  "image": "string (название модели)",
  "title": "string (путь к изображению)",
  "data": "string (описание)"
}
```

### Service (Услуги)
```json
{
  "_id": "string",
  "title": "string",
  "description": "string",
  "service": [{"title": "string", "price": "string"}],
  "addService": [{"title": "string", "price": "string"}],
  "signs": [{"title": "string", "description": "string"}],
  "advice": "string",
  "repair1": ["string"],
  "repair2": ["string"],
  "image": "string"
}
```

### Entry (Заявки)
```json
{
  "_id": "string",
  "name": "string",
  "phone": "string",
  "createdAt": "string"
}
```

## 🔧 Настройка

### Изменение порта сервера

В файле `server-json/index.js` измените:
```javascript
const port = 3004; // Ваш порт
```

### Изменение URL API в React приложении

В файлах компонентов измените базовый URL:
```typescript
const response = await axios.get("http://localhost:3004/categories");
```

## 📝 Решение проблемы с отсутствующими JSON файлами

Если у вас нет JSON файлов для сервера, используйте готовый файл `json/db.json`, который содержит все необходимые данные:
- 12 категорий услуг
- 12 моделей Mazda
- 11 различных услуг с детальной информацией
- Пустой массив для заявок (заполняется автоматически)

## 🎨 Особенности

- Полностью адаптивный дизайн
- Современный UI/UX
- Интеграция с формами
- Управление состоянием через Redux
- Типизация TypeScript
- Модульная структура компонентов

## 📦 Сборка для production

```bash
cd mazda-react
npm run build
```

Собранные файлы будут в папке `mazda-react/build`

## 🤝 Вклад

Это проект портфолио. Для демонстрации используйте JSON-сервер, так как он не требует дополнительных зависимостей.

## 📄 Лицензия

ISC

## 🎥 Демонстрация проекта

### Видео-демонстрация

📹 **Демонстрация работы сайта:**

<video width="800" height="450" controls>
  <source src="./demo/bandicam 2023-10-01 13-16-43-801.mp4" type="video/mp4">
  Ваш браузер не поддерживает видео тег. 
  <a href="./demo/bandicam 2023-10-01 13-16-43-801.mp4">Скачать видео</a>
</video>

**Примечание:** Если видео не отображается на GitHub, вы можете [скачать его напрямую](./demo/bandicam%202023-10-01%2013-16-43-801.mp4) или посмотреть локально.

---

#
## 👤 Автор

Проект создан для портфолио.

---

**Примечание:** Для демонстрации проекта рекомендуется использовать JSON-сервер (`server-json`), так как он не требует установки MongoDB и работает "из коробки" с готовыми данными.

